# foo&lt;A, B, C&gt;(x, y, z)

***Type parameters***

- **`A`** (`string | number`)
- **`B`** (`string | number`) – *Defaults to `string`.*
- **`C`** – *Defaults to `null`.*

***Parameters***

- **`x`** (`A`)
- **`y`** (`B`)
- **`z`** (`C`)

***Return type***

```
{
  x: A;
  y: B;
  z: C;
}
```
