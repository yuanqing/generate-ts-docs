### add(x, y)

Adds two numbers.

#### *Parameters*

- **`x`** (`number`) – First number.
- **`y`** (`number`) – Second number.

#### *Return type*

The result of adding `x` and `y`.

```
number
```
